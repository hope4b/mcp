# Bound MCP runtime agent context

## Scope

Project-instruction alignment at `mcp/main@1f9bd3bf0c8578649b7eaf020694a4d205830a7f`.
No runtime source, dependency, test, deploy, secret or realm change.

## Changes

- Removed whole `HANDOFF.md` and ambiguous latest `WORKLOG.md` from required
  pre-edit hydration and from required post-edit writes.
- Frozen both files as historical audit/coordination archives; exact bounded
  ranges remain available only for assigned historical research.
- Task-local notes now own current evidence, follow-up and next owner.
- Replaced stale local role names with resident `mcp-owner` plus explicit
  routing boundaries.
- Aligned project context, architecture map and test matrix with the current
  API-key runtime and current files (`api_resources.py`, agent contract and
  realm-agent modules).

The mandated project chain drops from 113,829 bytes to 9,510 bytes after these
edits. Historical archive bodies remain preserved; only leading policy notices
were added. No runtime behavior changed.
